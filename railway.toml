[build]
# Dockerfile build: nixpacks would not reliably install the Litestream binary.
builder = "dockerfile"

# The start command is the Dockerfile CMD (litestream replicate -exec uvicorn).
# DB_PATH must point at the Railway volume (/data/finance.db) and the
# LITESTREAM_* / R2 env vars must be set, or replication will fail.
